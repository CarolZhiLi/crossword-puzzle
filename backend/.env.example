# Backend environment example (copy to backend/.env and fill values)

# Flask secret
JWT_SECRET_KEY=change-this-secret

# Aiven MySQL connection
DB_HOST=mysql-xxxxxx-xxxxxx.h.aivencloud.com
DB_PORT=21404
DB_NAME=crswd
DB_USER=crswd
DB_PASSWORD=your-password-here

# TLS required by Aiven
DB_SSL_MODE=REQUIRED
# Path to Aiven CA certificate on disk
DB_SSL_CA=backend/certs/aiven-ca.pem

# Convenience DSN for SQLAlchemy (PyMySQL driver)
# Note: CA is typically provided via connect_args, not the URL.
DATABASE_URL=mysql+pymysql://crswd:your-password-here@mysql-xxxxxx-xxxxxx.h.aivencloud.com:21404/crswd
 
# Forgot password flow
FRONTEND_BASE_URL=http://localhost:8080
RESET_TOKEN_MINUTES=15

# Optional SMTP for sending reset emails
# For dev, services like Mailtrap work well
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=your-smtp-username
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=no-reply@your-domain.com
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false
# APP_NAME=CrossyThink
